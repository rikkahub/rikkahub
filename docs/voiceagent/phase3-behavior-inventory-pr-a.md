# Phase 3 PR-A behavior inventory

Source suites: VoiceConversationPersisterTest, HermesQueueRecordTest (master f96ee099).
Every row must end PR-A as either `covered: <NewSuite.newTestName>` or
`obsolete: <one-line reason naming the deleted internal>`. No blank dispositions.

## VoiceConversationPersisterTest (36 tests)

| # | Old test | Asserted behavior (one line, implementation-free) | Disposition |
|---|----------|---------------------------------------------------|-------------|
| 1 | `remove legacy voice session started notes removes only metadata tagged notes` | Removing legacy session-started notes deletes only assistant messages carrying the legacy voice-session-started metadata tag, leaving plain text mentioning the same phrase untouched. | (pending) |
| 2 | `append user tool and assistant turns creates visible Hermes tool record` | Appending a user turn, a Hermes tool call, and an assistant turn produces three messages (user, assistant-with-tool, assistant) where the tool part shows the call id, the "ask_hermes" tool name, a complete status, the prompt, and the answer text on both the tool and its output. | (pending) |
| 3 | `pending to complete upsert replaces matching call id in latest assistant message` | Completing a pending Hermes call with the same call id replaces the pending record in place rather than adding a second one, ending with a single tool record showing the updated prompt, complete status, and answer. | (pending) |
| 4 | `voice transcript upsert keeps streaming fragments in one visible turn` | Repeated partial transcript updates for the same turn id collapse into a single visible message per speaker showing only the latest text, with the assistant's final message flagged interrupted when the last update was interrupted. | (pending) |
| 5 | `upsert transcript keeps same turn id from different voice sessions` | A transcript update for the same turn id but a new session id is appended as a separate message rather than replacing the prior session's transcript. | (pending) |
| 6 | `upsert transcript with session id does not replace legacy transcript without session id` | A transcript entered without a session id is left alone by a later update for the same turn id that does carry a session id; both texts remain as separate messages. | (pending) |
| 7 | `user transcript upsert preserves partial and session closed statuses` | A partial user transcript update immediately followed by a session-closed-before-final update for the same turn collapses to one message showing the final text and a "session-closed-before-final" status. | (pending) |
| 8 | `assistant transcript upsert preserves partial complete and session closed statuses` | Assistant transcript updates for one turn id progress from partial to complete in a single message; a session-closed-before-final update under a different turn id lands as a separate message with that status. | (pending) |
| 9 | `voice transcript upsert replaces matching turn even when tool record interleaves` | A user transcript update still finds and updates its earlier turn even when a Hermes tool record was appended in between, and the tool record keeps its own place in the conversation. | (pending) |
| 10 | `complete upsert updates pending tool before newer assistant turn without appending duplicate` | Completing a pending Hermes call updates the tool record that sits before a later assistant turn, without inserting a new message or disturbing the assistant turn's position. | (pending) |
| 11 | `pending status records machine readable pending status on tool metadata` | A freshly created pending Hermes record shows a "pending" status on the tool and carries no output yet. | (pending) |
| 12 | `pending upsert with reused call id appends after completed Hermes record` | Starting a new pending Hermes call that reuses a call id already used by a completed call appends a second, separate tool record rather than reopening or replacing the completed one; both records remain visible with their own prompts, answers, and statuses. | (pending) |
| 13 | `terminal upsert with reused call id replaces latest Hermes terminal record` | Completing a Hermes call whose call id currently holds a failed terminal record replaces that record in place, leaving a single tool record with the new complete status and answer. | (pending) |
| 14 | `upsert ignores non Hermes tool with same call id` | A non-Hermes tool part sharing a call id with an incoming Hermes upsert is left untouched, and the Hermes record is added as an additional, separate tool part. | (pending) |
| 15 | `new call id after user turn appends tool message without changing old assistant` | A Hermes call with a new call id, made after a new user turn, is appended as its own message and does not add a tool part to the earlier assistant turn. | (pending) |
| 16 | `failed status records machine readable failure status as tool output metadata` | A failed Hermes record shows a "failed" status and the failure text on both the tool and its output part. | (pending) |
| 17 | `Hermes tool records preserve job id metadata across statuses` | A Hermes record's job id metadata is carried through from pending to complete for the same call id, and a different call id keeps its own separately supplied job id. | (pending) |
| 18 | `interrupted assistant turn records voice status metadata while preserving text` | An assistant turn marked interrupted keeps its spoken text intact while being flagged with an "interrupted" status. | (pending) |
| 19 | `voice artifacts include session source identifiers status and timestamps` | User transcript, assistant transcript, and Hermes tool records (and its output) all carry a common voice-agent source tag, the session id, an event id (turn id or call id), a status, non-blank created/updated timestamps, and — for the tool — the call id specifically. | (pending) |
| 20 | `Hermes queued and running statuses persist job metadata` | Queued and then running Hermes updates for the same call id show status, job id, and an unset ("false") announced flag; the created timestamp is preserved unchanged from queued to running while the updated timestamp advances. | (pending) |
| 21 | `job-keyed active upsert replaces legacy active record without job id` | An active (non-terminal) Hermes update carrying a job id replaces an earlier active record for the same call id that had no job id, keeping a single record and preserving its original created timestamp. | (pending) |
| 22 | `Hermes persisted status names are parseable queue statuses` | Every Hermes status (pending, queued, running, complete, failed, expired, canceled) is written to the tool metadata and its output metadata as a status name that round-trips back to the same queue status. | (pending) |
| 23 | `expired and canceled statuses persist terminal output metadata` | Expired and canceled Hermes records each show their status and message text on both the tool and its output, and the tool's and output's job id and status agree with each other. | (pending) |
| 24 | `terminal replay preserves existing announced flag by default` | Re-upserting the same complete Hermes result after it has been marked announced keeps the announced flag true on both the tool and its output rather than resetting it. | (pending) |
| 25 | `terminal upsert with new job id appends instead of replacing historical terminal record` | Completing a Hermes call under a new job id, when the call id already holds a completed record under an older job id, appends a second, separate terminal record instead of overwriting the historical one; both remain visible with their own job ids and answers. | (pending) |
| 26 | `terminal upsert without job id appends instead of replacing historical terminal record with job id` | A terminal Hermes update carrying no job id, arriving after a completed record with a job id for the same call id, is appended as a new record (with no job id metadata) rather than replacing the earlier one. | (pending) |
| 27 | `terminal upsert without job id does not replace active record with job id` | A terminal Hermes update with no job id does not replace a still-active record for the same call id that has a job id; both remain, the earlier one keeping its running status and job id, the new one holding the failed status without a job id. | (pending) |
| 28 | `active upsert without job id does not replace active record with job id` | A new active-status Hermes update with no job id does not replace an existing active record for the same call id that has a job id; both are kept as separate records. | (pending) |
| 29 | `terminal upsert with job id replaces matching active record not historical terminal record` | Completing a Hermes call under a job id replaces only the active record sharing that same job id, leaving an older completed record under a different job id untouched; both terminal records remain visible. | (pending) |
| 30 | `terminal upsert with job id replaces legacy active record without job id` | Completing a Hermes call under a job id replaces a legacy active record for the same call id that had no job id, ending with one record that has the new job id, status, answer, and the original created timestamp preserved. | (pending) |
| 31 | `mark Hermes result announced updates only matching terminal tool` | Marking one call's result announced flips the announced flag (and refreshes the updated timestamp) on only that call's tool and its output, leaving a different unannounced terminal call and a still-active call untouched by call id. | (pending) |
| 32 | `mark Hermes result announced updates only latest terminal record for reused call id` | For a call id reused across multiple upserts, marking the result announced affects only the most recent terminal record, leaving an older terminal record for the same call id still unannounced. | (pending) |
| 33 | `mark Hermes result announced can target reused call id by job id` | Marking announced by call id plus a specific job id affects only the terminal record carrying that job id, allowing each job under a reused call id to be announced independently. | (pending) |
| 34 | `mark Hermes result announced marks duplicate terminal records for same job id` | Marking announced by call id and job id marks every terminal record sharing that job id, including duplicate records for the same call id. | (pending) |
| 35 | `mark Hermes result announced for duplicate job id does not mark other job ids` | Marking announced by call id and job id leaves terminal records under a different job id for the same call id unannounced, even when a duplicate record shares the target job id. | (pending) |
| 36 | `mark Hermes result announced can target reused call id without job id` | Marking announced by call id while explicitly matching records without a job id affects only the jobless terminal record for that call id, leaving a newer record that does carry a job id unannounced. | (pending) |

## HermesQueueRecordTest (13 tests)

| # | Old test | Asserted behavior (one line, implementation-free) | Disposition |
|---|----------|---------------------------------------------------|-------------|
| 37 | `reads legacy pending Hermes records without announced metadata as active queue records` | A legacy pending Hermes tool record with no announced metadata is read as a single active queue record with pending status and its prompt, and appears in neither the unannounced nor announced terminal lists. | (pending) |
| 38 | `legacy terminal Hermes records without announced metadata are treated as announced` | Legacy complete and failed Hermes tool records with no announced metadata are treated as already-announced terminal results, appearing in the announced list and neither the active nor unannounced-terminal lists. | (pending) |
| 39 | `malformed Hermes metadata is skipped or defaulted without crashing queue parsing` | A Hermes record whose status metadata is a malformed shape is skipped from the read records entirely; a record with a valid status but malformed job id, announced flag, and timestamp metadata is still read, defaulting the job id and timestamps to absent and the announced flag to false. | (pending) |
| 40 | `malformed status with unannounced output recovers as failed terminal record` | A Hermes record with an unrecognized status string but explicit unannounced metadata is recovered as a failed terminal record, keeping its prompt and output text as the error message, and appears among the unannounced terminal results. | (pending) |
| 41 | `terminal records with malformed announcement metadata remain unannounced` | A terminal Hermes record whose announced-flag metadata is not a valid boolean is treated as unannounced, appearing in the unannounced list rather than the announced list. | (pending) |
| 42 | `queue parser skips non-Hermes tools and defaults malformed input prompts` | A tool part with a different tool name is excluded from the Hermes queue reading entirely; a Hermes record whose input is not valid JSON is still read, with its prompt defaulted to an empty string. | (pending) |
| 43 | `snapshot records expired and canceled terminal errors` | Expired and canceled Hermes records both appear among unannounced terminal results, each retaining its own prompt and its status message as the error text. | (pending) |
| 44 | `reads queued running and complete Hermes records from conversation` | Reading a conversation with queued, running, and complete Hermes calls yields one record per call id in order, each showing its own job id and status, the queued record's original prompt, and the complete record's answer with an unannounced flag. | (pending) |
| 45 | `queue snapshot separates active jobs and unannounced terminal results` | A snapshot separates an active (running) call, a completed call explicitly marked unannounced, and a completed call explicitly marked announced into three disjoint groups (active, unannounced terminal, announced terminal), each retaining its own prompt/job id/answer. | (pending) |
| 46 | `status question summary exposes active prompts and terminal counts without terminal content` | The status-question summary text names the durable queue status, lists the still-running call's prompt verbatim, and reports unannounced terminal results only as counts per outcome (completed/failed/expired/canceled) — never including the prompt or answer/error text of any terminal call. | (pending) |
| 47 | `status question summary is empty for announced terminal records only` | When the only Hermes record is a terminal result already marked announced, the status-question summary is an empty string. | (pending) |
| 48 | `queue snapshot uses latest record for duplicate durable identity` | When two Hermes tool parts share the same call id and job id, the queue snapshot resolves to the latest one only — the older duplicate does not appear as a separate active, unannounced, or announced record, and the surviving record's error text reflects the latest entry. | (pending) |
| 49 | `new terminal Hermes records default to unannounced queue results` | Freshly completed, failed, expired, and canceled Hermes records with no explicit announced flag default to unannounced terminal results, none appearing as active or already-announced. | (pending) |
