# Phase 3 PR-B behavior inventory

Source suite: HermesJobManagerTest (base 132df19f, tip of
`refactor/hermes-record-seam-29-35`, on which `refactor/hermes-job-reducer-28`
is stacked).
Disposition is `(pending)` for every row until the wholesale rewrite (Task 9)
reconciles each behavior against the new `HermesJobLifecycle` reducer suite.

## HermesJobManagerTest (82 tests)

| # | Old test | Asserted behavior (one line, implementation-free) | Disposition |
|---|----------|---------------------------------------------------|-------------|
| 1 | `submitted job keeps polling after session bridge detaches` | A job that completes after its bridge detaches is persisted as complete with the returned answer, left unannounced, produces no completion follow-up, and the job is never remote-cancelled. | (pending) |
| 2 | `submitted job persists pending before remote submit returns` | Before the remote submit call returns, the job is already persisted as a pending record with no job id; once the submit resolves it becomes a queued record carrying the job id. | (pending) |
| 3 | `submitted job records canonical prompt and answer observability events` | Submitting and completing a job emits observability events carrying the trace context, call id, job id, and the prompt/answer represented only as char count, sha256 hash, and truncation flag under namespaced keys, never as raw prompt/answer text or legacy key names. | (pending) |
| 4 | `cancel after pending persistence but before submit skips remote submit` | Cancelling immediately after the pending record is persisted, but before the remote submit call fires, ends with a canceled record and the remote submit is never issued. | obsolete: pinned the deleted pre-submit `explicitlyCanceled` short-circuit; the reducer emits `StartSubmit` unconditionally after `PersistPending` and handles a racing cancel by adopting the returned job id and cancelling remotely, so the remote submit now always fires (Task 8) |
| 5 | `hung submit request times out and expires pending record` | A submit call that hangs past the elapsed-time budget ends with an expired record carrying the polling-timed-out message, and the hung remote call is canceled. | (pending) |
| 6 | `submit failure persists failed pending record` | A submit call that throws persists a failed record carrying the exception message and performs no poll calls. | (pending) |
| 7 | `submit after terminal no job record with same call id creates fresh remote job` | Submitting a call id that already holds a failed record with no job id starts a brand-new remote job, ending with two records for that call id — the old jobless failed one and a new complete one carrying the fresh job id and answer. | (pending) |
| 8 | `same call id from different sessions can submit while older job remains active` | Submitting the same call id under two different session-scoped active keys while the first job is still active accepts both submits, issuing two separate remote requests with their own prompts. | (pending) |
| 9 | `same call id from same session is still treated as duplicate while active` | Re-submitting the same call id under the same session-scoped active key while the first job is still active is rejected as a duplicate and no second remote request is sent. | (pending) |
| 10 | `running submit status persists running record and keeps polling` | A submit response that already reports running status persists a running record carrying the job id and continues polling through to the eventual complete record. | (pending) |
| 11 | `terminal submit statuses persist terminal records without polling` | A submit response that already reports a terminal status (failed, expired, timeout, or canceled) persists directly as the matching terminal record (timeout mapping to Expired) with zero polls, and the attached bridge receives both a queued acknowledgement and a terminal follow-up with a non-blank reason for each. | (pending) |
| 12 | `terminal submit status remains unannounced when queued acknowledgement fails` | When the bridge's queued-acknowledgement send fails for a submit response that is already terminal, the record stays unannounced and no follow-up reaches that bridge; attaching a working bridge afterward delivers the terminal follow-up and marks the record announced. | (pending) |
| 13 | `queued acknowledgement is sent to attached bridge` | Submitting a job while a bridge is attached sends that bridge exactly one queued acknowledgement carrying the call id and session id. | (pending) |
| 14 | `blocking queued acknowledgement does not stop polling` | A queued acknowledgement send that blocks past the bridge send timeout does not stop the job's polling; the job still reaches a complete record with its answer. | (pending) |
| 15 | `detached bridge does not receive delayed queued acknowledgement` | If the bridge is detached while its queued-acknowledgement send is still pending, that acknowledgement is discarded and never recorded even after the pending send eventually completes. | (pending) |
| 16 | `throwing queued acknowledgement does not stop polling` | A queued acknowledgement send that throws does not stop the job from continuing to a complete record with its answer. | (pending) |
| 17 | `resume polls persisted active jobs and announces when bridge is attached` | Resuming with a persisted running record and an attached bridge polls the job to completion, ending with an announced complete record and exactly one completion follow-up carrying the call id, prompt, answer, and session id. | (pending) |
| 18 | `resume does not repeat already announced still working update` | Resuming a running job whose still-working announcement was already marked does not resend a still-working update even after the threshold elapses again, and the job still completes normally afterward. | (pending) |
| 19 | `explicit cancel cancels remote job and persists canceled` | Explicitly cancelling a queued job issues the remote cancel call and ends with a canceled record carrying the message "Hermes job canceled." | (pending) |
| 20 | `keyed cancel does not cancel another session with reused call id` | Cancelling by call id plus a specific session-scoped active key remote-cancels only the job started under that key, leaving another active job that reused the same call id under a different key to complete normally with its own answer. | (pending) |
| 21 | `remote cancel timeout keeps local canceled state without waiting for remote cleanup` | A remote cancel call that hangs past its own timeout still lets the local record settle into canceled status without waiting for the remote call to finish, and the remote-cancel attempt is left outstanding rather than marked as completed. | (pending) |
| 22 | `tool status callback emits actual completed call while another job remains active` | When one of two concurrently active jobs completes, the tool-status callback fires a HermesAnswered event naming the specific call id that finished, while the other job remains unaffected and can still complete independently afterward. | (pending) |
| 23 | `cancel after terminal result does not overwrite completed record` | Cancelling a call id after its job has already completed leaves the completed record and its answer untouched and never issues a remote cancel. | (pending) |
| 24 | `cancel while completed job is still unwinding does not overwrite completed record` | Cancelling a call id while its completion follow-up delivery is still in flight does not overwrite the already-complete record or its answer, and never triggers a remote cancel. | (pending) |
| 25 | `cancel before completion persistence wins over completed poll response` | A cancel issued while a completion's persistence is still blocked on the write lock wins over that in-flight completion — the only surviving record for the call id is canceled, and the completion's answer is never written out via the visible-answer callback. | obsolete: pinned the deleted `shouldPersist` race (cancel skipped the blocked terminal write); the actor serializes cancel as one event and the `Terminal` reducer state absorbs a late `CancelRequested`, so the completion now wins (Task 8) |
| 26 | `cancel before malformed success failure persistence wins over fallback failure` | A cancel issued while a malformed successful poll response's fallback-failure persistence is still blocked wins over that persistence — the only surviving record is canceled with the "Hermes job canceled." message, not the malformed-response failure text. | obsolete: pinned the deleted `shouldPersist` race; the malformed-success `Failed` terminal now commits and the late `CancelRequested` is absorbed by the `Terminal` state, so no remote cancel fires (Task 8) |
| 27 | `failed completion follow-up remains unannounced for later bridge retry` | When the attached bridge's completion follow-up send fails, the complete record stays unannounced and that bridge receives nothing; attaching a working bridge afterward delivers exactly one completion follow-up with the answer and marks the record announced. | (pending) |
| 28 | `failed terminal follow-up remains unannounced for later bridge retry` | When the attached bridge's terminal follow-up send fails for a failed job, the record stays unannounced and that bridge receives nothing; attaching a working bridge afterward delivers the terminal follow-up carrying the failure reason and marks the record announced. | (pending) |
| 29 | `successful follow-up is marked announced even if bridge detaches during mark` | Detaching the bridge while the just-delivered completion follow-up is still being marked announced does not stop the mark from committing — the record still ends up complete and announced, and the follow-up was delivered exactly once. | (pending) |
| 30 | `completion waiting behind detached bridge remains unannounced for later retry` | A newly completed job that arrives while an earlier unannounced completion's follow-up delivery to the same bridge is still blocked remains itself unannounced too; detaching the bridge before the block clears leaves the new job's record complete but unannounced, and only the original blocking call's follow-up is ever sent. | (pending) |
| 31 | `observer callback failures do not fail active Hermes job` | Every optional observer callback (tool-status, diagnostic, queue-event, answer-write) throwing an exception does not prevent the job from completing and persisting its answer normally. | (pending) |
| 32 | `poll timeout status persists expired record` | A poll response reporting a timeout status persists an expired record carrying the response's own error message. | (pending) |
| 33 | `polled running status persists running record and emits calling status` | A poll response reporting running status persists a running record and emits a CallingHermes tool-status event naming that call id, with the job later completing normally. | (pending) |
| 34 | `long running job announces still working once before completion follow-up` | A job that stays running past the still-working threshold sends exactly one still-working update to the bridge and persists the once-per-job announced flag, produces no further still-working updates across additional threshold windows, and still delivers exactly one completion follow-up when it finishes. | (pending) |
| 35 | `polled failed status persists failed record` | A poll that reports the job failed persists a failed record carrying the failure message. | (pending) |
| 36 | `polled failed status announces terminal result to attached bridge` | A poll that fails while a bridge is attached marks the record announced and delivers exactly one terminal follow-up naming the failed status and its reason. | (pending) |
| 37 | `polled failed status retries queued acknowledgement before terminal follow-up` | When the job's initial queued acknowledgement fails to send but later succeeds once acknowledgements are unblocked, and the job subsequently fails, the bridge ends up with exactly one recorded queued acknowledgement and exactly one terminal follow-up carrying the failure reason. | (pending) |
| 38 | `polled expired and canceled statuses announce terminal results to attached bridge` | A poll response that reports the remote job itself expired or was canceled persists the matching terminal status as announced and delivers exactly one terminal follow-up carrying that status and its reason, for both the expired and canceled cases. | (pending) |
| 39 | `polled expired status persists expired record` | A poll that reports the job expired persists an expired record carrying the expiration message. | (pending) |
| 40 | `hung poll request times out and cancels remote job` | A poll call that hangs past the elapsed-time budget ends with an expired record carrying the polling-timed-out message and issues a remote cancel for the hung job. | (pending) |
| 41 | `transient poll failure retries and persists eventual success` | A poll call that throws a plain exception once is treated as transient and retried, letting the job still reach a complete record with its answer. | (pending) |
| 42 | `legacy formatted poll failure is transient when it is not typed` | A poll failure carrying only a legacy-formatted error message (not a typed voice failure) is still treated as transient and retried, letting the job reach a complete record with its answer. | (pending) |
| 43 | `retryable typed poll failure retries and persists eventual success` | A poll failure carrying a typed voice-lab exception explicitly marked retryable is retried, letting the job reach a complete record with its answer. | (pending) |
| 44 | `terminal poll failure persists failed record without waiting for local timeout` | A poll failure carrying a typed voice-lab exception explicitly marked non-retryable ends the job immediately as failed — without waiting out the elapsed-time budget — persisting the formatted status-and-preview error text as announced and delivering exactly one terminal follow-up with that same reason. | (pending) |
| 45 | `terminal poll failure without typed failure persists failed record` | A poll failure carrying an HTTP exception with no typed failure classification still persists a failed record immediately, using the formatted status-code-and-preview text as the error. | (pending) |
| 46 | `unknown poll status persists failed record` | A poll response reporting an unrecognized status string persists a failed record whose error names the unrecognized status verbatim. | (pending) |
| 47 | `remote canceled poll status persists canceled record` | A poll response reporting the remote job as canceled persists a canceled record carrying the response's own error message. | (pending) |
| 48 | `resume expires already old active record without fresh timeout window` | Resuming an active record whose created timestamp is already older than the timeout budget expires it immediately rather than granting a fresh timeout window, and still issues a remote cancel for the job even though a poll would have returned a successful answer. | (pending) |
| 49 | `default durable age keeps nearly twenty four hour old resumed job active` | Resuming an active record whose age is just under the default 24-hour durability window still lets the job resume and complete normally, without issuing a remote cancel. | (pending) |
| 50 | `resume expires active record missing job id` | Resuming an active record that has no job id expires it immediately with the message "Hermes job was missing a job id." and makes no remote request at all. | (pending) |
| 51 | `resume expires active record with invalid timing metadata` | Resuming an active record whose created and updated timestamps are unparseable expires it immediately with the message "Hermes job had invalid timing metadata." and makes no remote request. | (pending) |
| 52 | `resume skips same manager active pending submit` | Calling resume while this same manager already has a submit in flight for a call id does not spawn a duplicate or expire that pending job — only one record ever exists for it, and it still completes normally. | (pending) |
| 53 | `resume skips same manager active pending submit with custom active key` | Calling resume while this same manager already has a submit in flight under a custom active key does not spawn a duplicate or expire that pending job — only one record and exactly one remote request exist for it, and it still completes normally. | (pending) |
| 54 | `resume does not start duplicate poller for live submitted job` | Calling resume while this manager's own submitted job is already mid-poll does not start a second poller for it — only one poll attempt is ever made, and the job still completes normally with its answer. | (pending) |
| 55 | `resume ignores stale active duplicate superseded by terminal record` | Resuming a call id whose persisted state has a stale active record alongside a newer terminal record for the same identity never polls the stale duplicate at all, leaving both records unchanged. | (pending) |
| 56 | `resume polls distinct active records when call id has multiple active jobs` | Resuming a call id that has two distinct active records under different job ids polls each one independently, and both reach their own complete record with their own answer. | (pending) |
| 57 | `job created side effects are skipped when submit loses to explicit cancel` | Cancelling a call id while its submit call is still in flight ends with a canceled record and an eventual remote cancel once the submit resolves, but the diagnostic and queue-event side effects normally tied to a job being created never fire. | (pending) |
| 58 | `cancel before submit returns cancels remote job after job id arrives` | Cancelling a call id while its submit call is still outstanding persists a canceled record right away, and once the submit later resolves with a job id, the remote cancel is issued against that job id — leaving exactly one record, canceled, carrying the resolved job id. | (pending) |
| 59 | `user cancel during in-flight submit marks canceled record announced under returned job id` | A user-initiated cancel of a job whose submit call is still returning its job id ends with exactly one canceled record carrying the late-arriving job id, already marked announced, and the remote job under that id is canceled; a later bridge re-attachment never replays a follow-up for it. | (pending) |
| 60 | `user cancel persist landing after submit canceled persist leaves no unannounced record` | However the ordering falls between a submit's own explicit-cancel persistence and a racing user-cancel persistence for the same job, every surviving record for that call id ends up canceled and announced, the remote job is canceled, and no spurious follow-up is ever replayed on a later bridge re-attachment. | obsolete: pinned the deleted multi-coroutine cancel-persist race via `HoldNextDispatchDispatcher.holdNextDispatch`, which deadlocks the single actor consumer (cancel is one serialized event, not a separately dispatched persistence coroutine) (Task 8) |
| 61 | `user cancel racing gemini cancel persists canceled record as announced` | When a user-initiated cancel and an independently triggered Gemini cancel race on the same already-queued job, the remote job is still canceled, every surviving record ends up canceled and announced, and no spurious follow-up is ever replayed on a later bridge re-attachment. | obsolete: pinned the same deleted `HoldNextDispatchDispatcher` dispatch-hold multi-coroutine race; dual cancels are now serialized events resolved by the reducer's `escalate` (Task 8) |
| 62 | `stale active poll does not resurrect terminal record` | A poll response that lands for a job whose durable record has already been externally overwritten to canceled does not resurrect or duplicate the record — exactly one record remains, still canceled. | (pending) |
| 63 | `repeated bridge attach announces each completed result once` | Attaching the same bridge twice for a call id with a pre-existing unannounced complete record sends exactly one completion follow-up, not one per attach, and marks the record announced. | (pending) |
| 64 | `completion announcement uses latest matching durable record` | When multiple complete records share the same call id and job id, attaching a bridge sends the completion follow-up using the latest record's prompt and answer rather than an older duplicate's, and marks every record sharing that identity as announced. | (pending) |
| 65 | `bridge attach ignores stale completed duplicate superseded by later terminal record` | When a newer terminal record supersedes an older complete duplicate for the same identity, attaching a bridge never sends a completion follow-up for the stale complete one — only the current terminal (failed) record is announced, with a single terminal follow-up carrying its own reason. | (pending) |
| 66 | `repeated bridge attach announces failed terminal result once` | Attaching the same bridge twice for a call id with a pre-existing unannounced failed record sends exactly one terminal follow-up, not one per attach. | (pending) |
| 67 | `concurrent completions preserve both durable records` | Two independent jobs completing concurrently while persistence writes are serialized both end up as separate complete records carrying their own answers — neither completion is lost or overwritten by the other. | (pending) |
| 68 | `queue store samples session id before waiting for every persistence update` | For every kind of Hermes persistence write (active, pending, canceled, terminal), the session id stamped on the record reflects the session that was current when the write was initiated, not a session id that changes later while the write is queued behind a concurrent blocked write. | (pending) |
| 69 | `appendVisibleResultMessageIfNeeded is a no-op for a terminal record already announced` | Calling the visible-result-message step for a terminal record that was persisted already announced (e.g., from a user-initiated cancel) is a no-op — it reports no message written, leaves messageWritten false, and never appends a visible chat message with the answer text. | (pending) |
| 70 | `resolveAndCancelRequest with nothing pending returns NothingPending` | Resolving a cancel-by-question request when there are no pending jobs at all reports NothingPending. | (pending) |
| 71 | `resolveAndCancelRequest cancels a single pending job even when the question does not match` | Resolving a cancel-by-question request when exactly one job is pending cancels that job even if the spoken question text doesn't match its prompt at all, remote-cancels it, and records a diagnostic naming its call id. | (pending) |
| 72 | `resolveAndCancelRequest matches bidirectionally after normalization` | Resolving a cancel-by-question request with multiple jobs pending matches the one whose prompt equals the spoken question after case and whitespace normalization, canceling only that one. | (pending) |
| 73 | `resolveAndCancelRequest matches when the question contains the whole prompt` | Resolving a cancel-by-question request matches a pending job whose entire prompt appears as a substring within the longer spoken question, canceling only that one. | (pending) |
| 74 | `resolveAndCancelRequest collapses whitespace inside the matched span` | Resolving a cancel-by-question request still matches a pending job whose prompt contains extra internal whitespace against a spoken question with that whitespace collapsed. | (pending) |
| 75 | `resolveAndCancelRequest reports NoMatch with the pending list` | Resolving a cancel-by-question request against an unrelated question reports NoMatch carrying the full list of currently pending call ids, and cancels neither job. | (pending) |
| 76 | `resolveAndCancelRequest reports Ambiguous with the matching subset` | Resolving a cancel-by-question request that matches more than one pending job's prompt reports Ambiguous carrying just the matching subset of call ids, and cancels none of them. | (pending) |
| 77 | `handleCancelHermesCall sends the outcome through the bridge and records sent` | Handling an incoming cancel-Hermes-call request that resolves to a cancellation sends the outcome through the attached bridge tagged with the request's own call id, and records a response-sent diagnostic. | (pending) |
| 78 | `handleCancelHermesCall records failed when the bridge send returns false` | When the attached bridge's cancel-response send returns false, handling the cancel request records a response-failed diagnostic citing the request's call id and a send_returned_false reason. | (pending) |
| 79 | `handleCancelHermesCall records send_timeout when the bridge send exceeds the timeout` | When the attached bridge's cancel-response send exceeds the send timeout, handling the cancel request records a response-failed diagnostic with a send_timeout reason. | (pending) |
| 80 | `handleCancelHermesCall records no_bridge_attached when no bridge is attached` | Handling a cancel request with no bridge attached at all records a response-failed diagnostic reporting no session and a no_bridge_attached reason, and sends nothing anywhere. | (pending) |
| 81 | `handleCancelHermesCall records session_mismatch when the attachment session differs` | Handling a cancel request whose session id doesn't match the currently attached bridge's session records a response-failed diagnostic citing the attached session and a session_mismatch reason, and sends nothing to that bridge. | (pending) |
| 82 | `handleCancelHermesCall reports delivered response as sent with attachmentChanged advisory` | When the bridge is detached and re-attached with the same session while its cancel-response send is still in flight, the response is still delivered exactly once and recorded as sent, carrying an attachmentChanged=true advisory rather than being treated as a failure. | (pending) |
